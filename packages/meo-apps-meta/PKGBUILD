pkgname=meo-apps-meta
pkgver=2026.08
pkgrel=1
pkgdesc="MeoArch official applications integration package"
arch=('any')
license=('MIT')
depends=('meo-account' 'meo-settings' 'omnistore-bin' 'flatpak')
source=()
sha256sums=()

package() {
  install -Dm644 /dev/stdin "$pkgdir/usr/share/licenses/$pkgname/package-note" <<'EOF'
This dependency-only package installs the supported MeoArch applications.
EOF
}
